<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.databox.mirror">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".base.BaseApplication"
        android:theme="@style/Theme.mirror">

        <activity android:name=".function.login_note.LoginNoteActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".function.register.RegisterActivity"/>
        <activity android:name=".function.login_passward.LoginPassWordActivity"/>
<!--    <activity android:name=".function.login_rider.LoginRiderActivity"/>-->
        <activity android:name="com.databox.mirror.function.main.MainActivity"
            tools:ignore="DuplicateActivity" />

    </application>

</manifest>